extends layout.pug
block head
  mate(cherset="UTF-8")
  //- link(rel="stylesheet" href="/styles/login.css")
  title ログイン
block content
  .row
    .col-md-6.col-md-offset-3
      form(action="/new_toot" method="post")
        .form-group
          textarea.form-control(name="tootl" placeholder="今何してる？" required)
        .form-group
          input.btn.btn-primary.btn-lg(type="submit" value="toot!" required)
        p